apiVersion: apps/v1
kind: Deployment
metadata:
  name: products-service-blue
  labels:
    app: products-service-blue
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  selector:
    matchLabels:
      app: products-service-blue
  template:
    metadata:
      labels:
        app: products-service-blue
    spec:
      containers:
      - name: products-service-blue
        image: path/ishara-usoof/products:latest
        ports:
        - containerPort: 8002


