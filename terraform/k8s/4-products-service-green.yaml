apiVersion: v1
kind: Service
metadata:
  name: products-service-green
spec:
  type: LoadBalancer
  ports:
  - protocol: TCP
    port: 8002
    targetPort: 8002
  selector:
    app: products-service-green
